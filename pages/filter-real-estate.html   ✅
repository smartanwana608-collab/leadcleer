<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Filter Real Estate Agents | CSV Automation Tools</title>

  <meta name="description" content="Upload a CSV file to identify and separate real estate agents using email-based detection." />
  <meta name="robots" content="index, follow" />

  <style>
    /* RESET */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: #f4f6f8;
      color: #1f2937;
      line-height: 1.6;
    }

    :root {
      --primary: #2563eb;
      --gray: #6b7280;
      --border: #e5e7eb;
      --card: #ffffff;
    }

    /* HEADER */
    header {
      background: #ffffff;
      border-bottom: 1px solid var(--border);
      padding: 20px;
      text-align: center;
    }

    header h1 {
      font-size: 1.6rem;
      margin-bottom: 4px;
    }

    header p {
      color: var(--gray);
      font-size: 0.95rem;
    }

    /* MAIN */
    main {
      max-width: 1000px;
      margin: 40px auto;
      padding: 0 20px;
    }

    /* CARD */
    .card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 32px;
      margin-bottom: 30px;
    }

    .card h2 {
      font-size: 1.3rem;
      margin-bottom: 10px;
    }

    .card p {
      color: var(--gray);
      margin-bottom: 20px;
    }

    /* UPLOAD */
    .upload-box {
      border: 2px dashed var(--border);
      border-radius: 12px;
      padding: 40px 20px;
      text-align: center;
    }

    .upload-box input {
      margin-top: 15px;
    }

    .upload-hint {
      font-size: 0.85rem;
      color: var(--gray);
      margin-top: 8px;
    }

    /* FILE INFO */
    .file-info {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 16px;
      margin-top: 20px;
    }

    .info-item {
      background: #f9fafb;
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 16px;
      font-size: 0.9rem;
    }

    /* TABLE */
    .table-wrapper {
      margin-top: 20px;
      overflow-x: auto;
      border: 1px solid var(--border);
      border-radius: 10px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.9rem;
    }

    th, td {
      padding: 12px;
      border-bottom: 1px solid var(--border);
      text-align: left;
      white-space: nowrap;
    }

    th {
      background: #f9fafb;
      font-weight: 600;
    }

    /* ACTION */
    .action {
      margin-top: 30px;
      text-align: right;
    }

    .btn {
      background: var(--primary);
      color: #ffffff;
      padding: 14px 26px;
      border-radius: 10px;
      border: none;
      font-size: 15px;
      font-weight: 600;
      cursor: not-allowed;
      opacity: 0.6;
    }

    /* FOOTER */
    footer {
      text-align: center;
      font-size: 0.85rem;
      color: var(--gray);
      padding: 20px;
    }

    footer a {
      color: var(--primary);
      text-decoration: none;
      margin-left: 8px;
    }

    @media (max-width: 600px) {
      header h1 {
        font-size: 1.4rem;
      }
    }
  </style>
</head>

<body>

<header>
  <h1>Filter Real Estate Agents</h1>
  <p>Upload a CSV file to detect and separate real estate agents automatically.</p>
</header>

<main>

  <!-- UPLOAD CARD -->
  <div class="card">
    <h2>Upload CSV File</h2>
    <p>Select your contact CSV file. All columns will be preserved.</p>

    <div class="upload-box">
      <strong>Choose a CSV file</strong><br />
      <input type="file" accept=".csv" disabled />
      <div class="upload-hint">CSV only · Data stays in your browser</div>
    </div>
  </div>

  <!-- FILE INFO -->
  <div class="card">
    <h2>File Overview</h2>
    <p>Summary of the uploaded file.</p>

    <div class="file-info">
      <div class="info-item">File name: —</div>
      <div class="info-item">Total rows: —</div>
      <div class="info-item">Columns detected: —</div>
      <div class="info-item">Email column: —</div>
    </div>
  </div>

  <!-- PREVIEW -->
  <div class="card">
    <h2>Data Preview</h2>
    <p>First few rows from your CSV file.</p>

    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>Column 1</th>
            <th>Column 2</th>
            <th>Column 3</th>
            <th>...</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Sample</td>
            <td>Sample</td>
            <td>Sample</td>
            <td>...</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="action">
      <button class="btn">Analyze CSV</button>
    </div>
  </div>

</main>

<footer>
  © 2026 CSV Automation Tools
  <a href="../index.html">Back to tools</a>
</footer>

</body>
</html>
